# Local environment files (NEVER commit these)
.env
*.env
.env.local

# GCP service account keys (CRITICAL - NEVER commit!)
gcp-key.json
**/gcp-key.json
*.json

# Kubernetes specific
kubeconfig
*.kubeconfig
certs/
*.pem
*.crt
*.key

# Secret backups and temporary files
sealed-secrets-key.yaml
/tmp/secret.yaml
secret.yaml
*-secret.yaml.tmp
*-secrets-backup.yaml

# OS & IDE
.DS_Store
.idea/
.vscode/
node_modules/

# Helm & Temporary files
*.log
charts/*/charts/
charts/*/tmp/